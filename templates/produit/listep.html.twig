{% block body %}
    <h1>Liste des produits</h1>

    <table class="table table-condensed">
        <thead>
            <tr>
                <th>Id</th>
                <th>Libelle</th>
                <th>Catégorie</th>
                <th>Pa</th>
                <th>Pv</th>
                <th>Tva</th>
              

            </tr>
        </thead>
        <tbody>
        {% for produit in produits %}
            <tr>
                <td>{{ produit.id }}</td>
                <td>{{ produit.libelle }}</td>
                <td>{{ produit.categorie.nom }}</td>
                <td>{{ produit.Pa }}</td>
                <td>{{ produit.Pv }}</td>
                <td>{{ produit.Tva }}</td>
              

                
            </tr>
        {% else %}
            <tr>
                <td colspan="8">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('produit_new') }}">Create</a>
{% endblock %}
